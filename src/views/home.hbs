<section>
    <section>
        Users
        <ul>
            {{#each users}}
            <li>
                <h3>{{user_name}}</h3>
                <image src={{user_image}} />
                <p>Following</p>
                {{following_list}}
            </li>
            {{/each}}
        </ul>
    </section>
</section>